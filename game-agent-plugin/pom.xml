<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>net.lumina</groupId>
  <artifactId>lumina-game-agent</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <!-- Java 8 target so ONE jar runs on every server JVM from legacy 1.8 servers (Java 8) to
         modern ones (Java 21+). The plugin uses only JDK 1.1+ APIs (HttpURLConnection) plus the
         Bukkit API, so there are no runtime dependencies to shade. -->
    <maven.compiler.source>8</maven.compiler.source>
    <maven.compiler.target>8</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <repositories>
    <repository>
      <id>spigot-repo</id>
      <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
    </repository>
  </repositories>

  <dependencies>
    <!-- Compiled against the Spigot/Bukkit API, which Paper, Purpur and Spigot all implement — so
         this single artifact loads on all of them. `provided`: the server supplies it at runtime. -->
    <dependency>
      <groupId>org.spigotmc</groupId>
      <artifactId>spigot-api</artifactId>
      <version>1.20.1-R0.1-SNAPSHOT</version>
      <scope>provided</scope>
    </dependency>
    <!-- Log4j2 core: the real server console. Bukkit/Paper have shipped Log4j2 since 1.7, so
         capturing the console means attaching an appender to the Log4j2 root — a JUL handler on
         Bukkit.getLogger() only sees that one logger, not the console. `provided`: the server has it. -->
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-core</artifactId>
      <version>2.19.0</version>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>LuminaGameAgent</finalName>
  </build>
</project>
